<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required on Android 11+ to discover wallet apps for deep linking -->
    <queries>
        <!-- Solana Mobile Wallet Adapter protocol -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="solana-wallet" />
        </intent>
        <!-- Phantom wallet -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="phantom" />
        </intent>
        <package android:name="app.phantom" />
        <!-- Solflare wallet -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="solflare" />
        </intent>
        <package android:name="com.solflare.mobile" />
        <!-- Jupiter Mobile -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="jupiter" />
        </intent>
        <package android:name="ag.jup.jupiter.android" />
        <!-- WalletConnect -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="wc" />
        </intent>
        <!-- Generic intent scheme (for intent:// URIs) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="intent" />
        </intent>
        <!-- HTTPS links (for fallback store links) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Solana Saga"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
